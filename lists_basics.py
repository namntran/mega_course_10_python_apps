# create lists of numbers using a range
student_grades = [9.1, 8.8, 7.5]

mysum = sum(student_grades)
length = len(student_grades)
mean = mysum/ length
print("the mean of the student grades is", mean)
